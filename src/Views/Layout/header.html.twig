
<header>
  <div class="topBar">
    <div class="helper">
      <span class="logoText"><a class="logoLink" href="{{url('Index')}}">Lux<span class="colorRed">Car</span></a></span>
      <span class="topBarItems">
        {% if not firstName %}
          <a class="topBarItem" href="{{url('login')}}">Logowanie</a>
          <a class="topBarItem" href="{{url('register')}}">Rejestracja</a>
        {% else %}
          <span class="topBarText">Witaj {{firstName}}</span>
          <a class="topBarItem" href="{{url('logout')}}">Wyloguj</a>
        {% endif %}
      </span>
      <span class="burgerPosition">
        <span id="toggle" onClick="onClickMenu()">&#9776;</span>
      </span>
    </div>
    <nav id="navigation" class="nav">
      <a class="item" href="{{url("contact")}}">KONTAKT</a>
      <a class="item" href="{{url("aboutCompany")}}">O FIRMIE</a>
      <a class="item" href="{{url("offer")}}">OFERTA</a>
      {% if firstName %}
        <a class="item" href="{{url("profile")}}">MOJE KONTO</a>
      {% endif %}
      <a class="item" href="{{url("Index")}}">STRONA GŁÓWNA</a>
    </nav>
  </div>
</header>
